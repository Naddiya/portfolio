import React from 'react';
import Fade from 'react-reveal/Fade';

const Stacks = () => (
  <Fade bottom duration={1000} delay={300} distance="0px">
    <h2 className="section-Stacks">STACK LIST</h2>
  </Fade>
);

export default Stacks;
